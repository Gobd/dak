{
  "name": "dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:relay": "concurrently -n vite,relay -c blue,green \"vite\" \"uv run --directory services/home-relay python relay.py\"",
    "build": "tsc -b && vite build",
    "preview": "vite build --mode development && vite preview --port 8080",
    "lint:sh": "shellcheck scripts/*.sh",
    "typecheck": "tsc --noEmit",
    "check": "pnpm lint:sh && pnpm typecheck && pnpm build",
    "analyze": "ANALYZE=true vite build && echo '\nBundle analysis generated: stats.html'",
    "gen:api": "cd services/home-relay && uv run python -c \"from app.main import app; import json; print(json.dumps(app.openapi()))\" > /tmp/openapi.json && cd ../.. && npx @hey-api/openapi-ts -i /tmp/openapi.json -o src/api/generated -c @hey-api/client-fetch",
    "gen:api:check": "pnpm gen:api && git diff --exit-code src/api/generated || (echo 'Error: Generated API client is out of sync. Run pnpm gen:api and commit.' && exit 1)"
  },
  "dependencies": {
    "@dak/kasa-client": "workspace:*",
    "@dak/ui": "workspace:*",
    "@tanstack/react-query": "catalog:",
    "lucide-react": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "zustand": "catalog:"
  },
  "devDependencies": {
    "@dak/vite-shared-react": "workspace:*",
    "@hey-api/openapi-ts": "^0.90.4",
    "@tailwindcss/vite": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "concurrently": "catalog:",
    "globals": "catalog:",
    "rollup-plugin-visualizer": "catalog:",
    "shellcheck": "^4.1.0",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
