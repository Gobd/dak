# TODO

- [ ] Add Google login (iOS/Android/Web) - use `expo-auth-session` + Supabase OAuth provider
- [ ] Add note locking for shared notes - prevent concurrent edits by showing "X is editing" and blocking edits until lock released (prevents conflict overwrites)

Remove account toggle soon just do in db

bathroom ikea medicine cabinet
15.5in wide
5.5in deep
37in tall
