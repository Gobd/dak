{
  "name": "dak",
  "private": true,
  "volta": {
    "node": "24.12.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "format:check": "pnpm -r format:check",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "check": "pnpm -r check"
  }
}
