{
  "name": "dak",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "volta": {
    "node": "24.12.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "format:check": "pnpm -r format:check",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "build:pages": "pnpm build && rm -rf _site && mkdir -p _site && cp _site_index.html _site/index.html && cp -r dashboard/dist _site/dashboard && cp -r family-chores/dist _site/family-chores && cp -r health-tracker/dist _site/health-tracker && cp -r notes-app/dist _site/notes-app"
  }
}
